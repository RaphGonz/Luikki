---
phase: 01-foundation-project-palette-pipeline-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_pipeline.py
  - tests/test_extract.py
  - tests/test_web/__init__.py
  - tests/test_web/conftest.py
  - tests/test_web/test_project_routes.py
  - tests/test_web/test_page_routes.py
  - tests/test_web/test_palette_routes.py
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05, PAL-01, PAL-02, PAL-03]
user_setup: []

must_haves:
  truths:
    - "`pytest` collects the whole suite without an ImportError, including the new scaffolded files."
    - "Every Wave 0 test file named in 01-VALIDATION.md exists on disk with named, skip-marked stubs."
    - "`scikit-image` is a declared dependency instead of an undeclared transitive import (RESEARCH.md Runtime State Inventory)."
    - "The web dependency set (fastapi, uvicorn, python-multipart) is installable via a declared `web` extra."
  artifacts:
    - path: "pyproject.toml"
      provides: "scikit-image core dep, [project.optional-dependencies].web extra, httpx in dev"
      contains: "scikit-image"
    - path: "tests/test_pipeline.py"
      provides: "PROJ-04 stub tests for the stage registry (filled by plan 01-04)"
      min_lines: 25
    - path: "tests/test_extract.py"
      provides: "PAL-01/PAL-02 stub tests for palette extraction (filled by plan 01-05)"
      min_lines: 25
    - path: "tests/test_web/conftest.py"
      provides: "TestClient + temp-project + PNG-bytes fixtures shared by every route test"
      min_lines: 30
    - path: "tests/test_web/test_project_routes.py"
      provides: "PROJ-01/PROJ-05 stub tests (filled by plan 01-06)"
    - path: "tests/test_web/test_page_routes.py"
      provides: "PROJ-02/PROJ-04 stub tests (filled by plan 01-07)"
    - path: "tests/test_web/test_palette_routes.py"
      provides: "PAL-01/PAL-03/PAL-04 + PROJ-03/PAL-02 stub tests (filled by plans 01-08, 01-09)"
  key_links:
    - from: "tests/test_web/conftest.py"
      to: "comiccolor.web.app.create_app"
      via: "lazy import inside the client fixture body"
      pattern: "from comiccolor.web.app import create_app"
    - from: "pyproject.toml"
      to: "fastapi/uvicorn/python-multipart"
      via: "[project.optional-dependencies].web"
      pattern: "fastapi"
---

<objective>
Wave 0. Put the dependency declarations and the test scaffolding in place so every
later plan in this phase has something to assert against, and so `pytest` never
breaks at collection time on modules that do not exist yet.

This plan writes no production code. It closes the "Wave 0 Requirements" checklist
in `01-VALIDATION.md` for the Python side (the frontend side is plan 01-02).

Purpose: 01-VALIDATION.md marks every backend test file for this phase as
`❌ Wave 0`. Implementation plans that also have to invent their own test file
and its fixtures produce shallower tests. Declaring the file names, the fixture
surface and the test names up front makes each later plan a fill-in, not an
invention.

Output: an updated `pyproject.toml` and six new test files whose stubs are all
skip-marked and named.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-palette-pipeline-backbone/01-VALIDATION.md
@.planning/phases/01-foundation-project-palette-pipeline-backbone/01-RESEARCH.md
@.planning/phases/01-foundation-project-palette-pipeline-backbone/01-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols and files created by **this plan** (new — not pre-existing, do not flag as drift):

| Artifact | Kind | File |
|---|---|---|
| `[project.optional-dependencies].web` | pyproject extra | `pyproject.toml` |
| `tests/test_pipeline.py` | test module | new file |
| `tests/test_extract.py` | test module | new file |
| `tests/test_web/` | test package | new directory |
| `project_dir`, `client`, `blank_client`, `make_png` | pytest fixtures | `tests/test_web/conftest.py` |
| `test_stage_chain_is_declared_in_order`, `test_only_import_has_a_runner`, `test_every_stage_except_import_has_an_upstream`, `test_run_import_advances_page_to_panels`, `test_stage_lookup_by_name` | test stubs | `tests/test_pipeline.py` |
| `test_flat_chip_grid_recovers_exact_colours`, `test_adaptive_count_collapses_near_duplicates`, `test_sheet_prepass_drops_ink_and_paper`, `test_noisy_gradient_returns_a_sane_count`, `test_thresholds_are_named_constants` | test stubs | `tests/test_extract.py` |
| `test_create_project_makes_folder_layout`, `test_reopen_after_close_keeps_pages_and_palette`, `test_requests_without_an_open_project_return_409`, `test_open_rejects_a_path_without_a_project_db`, `test_recent_list_skips_folders_that_disappeared`, `test_close_checkpoints_the_wal` | test stubs | `tests/test_web/test_project_routes.py` |
| `test_upload_stores_a_server_generated_filename`, `test_upload_rejects_a_non_image_with_4xx`, `test_stage_field_is_panels_after_upload`, `test_adding_pages_later_preserves_existing_pages`, `test_volume_crud_round_trip`, `test_pipeline_stages_endpoint_lists_eight_stages` | test stubs | `tests/test_web/test_page_routes.py` |
| `test_swatch_upload_creates_colour_n_entries`, `test_hand_crud_round_trip`, `test_recolour_reports_affected_pages_and_leaves_stage_alone`, `test_sheet_upload_returns_unpersisted_proposals`, `test_accept_creates_entity_and_labelled_entries`, `test_reject_discards_the_pending_sheet`, `test_sheet_id_with_path_separators_is_rejected` | test stubs | `tests/test_web/test_palette_routes.py` |

<tasks>

<task type="auto">
  <name>Task 1: Declare the phase's Python dependencies</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current state — 4 core deps, one `dev` extra, pytest config)
    - .planning/phases/01-foundation-project-palette-pipeline-backbone/01-RESEARCH.md § Standard Stack (verified versions), § Runtime State Inventory (build-artifacts row states the scikit-image debt), § Package Legitimacy Audit
  </read_first>
  <action>
Add `"scikit-image>=0.26"` to `[project].dependencies`. It is already imported at
runtime by `src/comiccolor/segmentation/closure.py` (`skimage.morphology.skeletonize`)
and is installed in `.venv` at 0.26.0 but has never been declared — RESEARCH.md's
Runtime State Inventory names this as a debt to settle while touching the file.
`colour/extract.py` (plan 01-05) will also use `skimage.color.rgb2lab` and
`skimage.color.deltaE_cie76`.

Add a new `[project.optional-dependencies]` group named `web` containing exactly
`"fastapi>=0.141"`, `"uvicorn[standard]>=0.52"`, `"python-multipart>=0.0.32"`.
The `fastapi>=0.141` floor is load-bearing: `app.frontend()` (used by plan 01-06)
only exists from 0.141.0.

Extend the existing `dev` extra to `["pytest>=8.0", "httpx>=0.28"]` — `httpx` backs
`fastapi.testclient.TestClient`, which every `tests/test_web/*` file uses.

Do not add Pylette, colorthief, colorgram.py, Konva, or any component framework —
RESEARCH.md § Alternatives Considered and § Anti-Patterns rule each of them out for
this phase.

All five packages appear in RESEARCH.md's Package Legitimacy Audit with an
`Approved` disposition and no `[SUS]`/`[SLOP]` verdicts (the `python-multipart`
naming flag was investigated and resolved as a slopcheck false positive — it is
FastAPI's own documented upload dependency), so no legitimacy checkpoint is
required before installing.

Install into the existing virtualenv with
`.venv/Scripts/python.exe -m pip install -e ".[dev,web]"` (Windows dev machine;
use `.venv/bin/python` if the layout differs).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pip install -e ".[dev,web]" && .venv/Scripts/python.exe -c "import fastapi, uvicorn, multipart, httpx, skimage; print(fastapi.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[project].dependencies` contains a line matching `scikit-image>=0.26`
    - `pyproject.toml` contains a `web` key under `[project.optional-dependencies]` whose value includes `fastapi>=0.141`, `uvicorn[standard]>=0.52` and `python-multipart>=0.0.32`
    - `pyproject.toml` `dev` extra includes `httpx>=0.28`
    - `.venv/Scripts/python.exe -c "import fastapi; print(fastapi.__version__)"` prints a version `>= 0.141`
    - `.venv/Scripts/python.exe -c "from fastapi import FastAPI; print(hasattr(FastAPI(), 'frontend'))"` prints `True` (confirms `app.frontend()` is available for plan 01-06; if it prints `False`, record it in the SUMMARY so plan 01-06 takes the documented `StaticFiles(html=True)` fallback)
    - `pytest -q` still exits 0 on the pre-existing suite
  </acceptance_criteria>
  <done>The web/imaging dependency set is declared and importable from the project venv, and the pre-existing test suite is still green.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the pipeline and extraction test modules</name>
  <files>tests/test_pipeline.py, tests/test_extract.py</files>
  <read_first>
    - tests/test_store.py (fixture style: `@pytest.fixture def store(tmp_path)` with `Store` as a context manager; docstring convention "what design guarantee this proves")
    - tests/test_masks.py (synthetic-numpy-array style for pure-function tests)
    - .planning/phases/01-foundation-project-palette-pipeline-backbone/01-VALIDATION.md § Wave 0 Requirements
    - .planning/phases/01-foundation-project-palette-pipeline-backbone/01-RESEARCH.md § Architecture Patterns Pattern 3, Pattern 4, Pattern 5, and § Code Examples (the verified 4-chip recovery test)
    - .planning/codebase/TESTING.md
  </read_first>
  <action>
Create `tests/test_pipeline.py` and `tests/test_extract.py` as scaffolds only.

**Stub shape (applies to every stub in this plan):** each stub is a `def test_*`
function decorated with `@pytest.mark.skip(reason="Wave 0 scaffold — filled by plan 01-NN")`,
carrying the real docstring the finished test will keep, and a body of `...`.
Every `comiccolor.*` import must live *inside* the function body, never at module
level, so `pytest --collect-only` never fails on `comiccolor.pipeline` /
`comiccolor.colour` / `comiccolor.web` not existing yet. Only `pytest`, `numpy`
and `PIL` may be imported at module level.

`tests/test_pipeline.py` — module docstring in the voice of `tests/test_store.py`
("The stage-chain contract, as tests"), citing D-06/D-07/D-10/D-11. Stubs, all
marked `filled by plan 01-04`:
  - `test_stage_chain_is_declared_in_order` — STAGES declares import, panels,
    protected, zones, propose, snap, review, export in that order (D-11).
  - `test_only_import_has_a_runner` — exactly one Stage has a non-None runner (D-11).
  - `test_every_stage_except_import_has_an_upstream` — the chain is fully linked
    and `IMPORT.upstream is None` (D-10).
  - `test_run_import_advances_page_to_panels` — `run_import` moves a freshly added
    page's `stage` to `PipelineStage.PANELS` and persists it (D-07).
  - `test_stage_lookup_by_name` — `stage_for` resolves a `PipelineStage` to its
    `Stage` and raises `KeyError` for an unknown name.

`tests/test_extract.py` — module docstring citing D-12/D-13/D-14/D-15. Stubs, all
marked `filled by plan 01-05`:
  - `test_flat_chip_grid_recovers_exact_colours` — docstring quotes D-12
    ("if it was already an image with chips ... 100% of the time"); the finished
    test builds the 40x40 four-chip array from RESEARCH.md § Code Examples.
  - `test_adaptive_count_collapses_near_duplicates` — two chips one CIELAB step
    apart come back as one entry, so the count is adaptive rather than K_MAX (D-15).
  - `test_sheet_prepass_drops_ink_and_paper` — with `sheet_mode=True`, near-black
    and near-white pixels contribute no entry (D-14).
  - `test_noisy_gradient_returns_a_sane_count` — a smooth gradient returns
    at least 1 and fewer than K_MAX entries (RESEARCH.md Pitfall 5's second regime).
  - `test_thresholds_are_named_constants` — asserts `K_MAX`, `MERGE_DELTA_E`,
    `MIN_PIXEL_SHARE`, `INK_MAX` and `PAPER_MIN` are importable module-level names,
    pinning RESEARCH.md Pitfall 5's "no unvalidated magic numbers" rule.

Do **not** scaffold `tests/test_masks.py` — it already exists and imports from
`comiccolor.model.masks` at module level; plan 01-04 adds its invariant tests at
the same time as the code they exercise.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_pipeline.py tests/test_extract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_pipeline.py tests/test_extract.py -q` exits 0 and reports 10 skipped, 0 failed, 0 errors
    - `pytest --collect-only -q tests/` exits 0 (no ImportError from the new files)
    - `grep -c "pytest.mark.skip" tests/test_pipeline.py` returns 5
    - `grep -c "pytest.mark.skip" tests/test_extract.py` returns 5
    - `grep -n "^from comiccolor" tests/test_pipeline.py tests/test_extract.py` returns no matches (all project imports are function-local)
    - Both files contain a module docstring citing at least one D-NN decision id
  </acceptance_criteria>
  <done>Both test modules exist, collect cleanly, and every stub is skip-marked with the plan number that will fill it.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the web route test package and its shared fixtures</name>
  <files>tests/test_web/__init__.py, tests/test_web/conftest.py, tests/test_web/test_project_routes.py, tests/test_web/test_page_routes.py, tests/test_web/test_palette_routes.py</files>
  <read_first>
    - tests/test_store.py (the `tmp_path` + context-manager fixture convention this package reuses)
    - .planning/phases/01-foundation-project-palette-pipeline-backbone/01-RESEARCH.md § Validation Architecture (TestClient is httpx-backed, no uvicorn process), § Code Examples (per-request Store dependency), Pitfall 2 (`app.state.current_project_path` is process-global)
    - .planning/phases/01-foundation-project-palette-pipeline-backbone/01-VALIDATION.md § Requirement → Test Map (the exact test ids other documents already reference, e.g. `test_palette_routes.py::test_sheet_upload`, `test_project_routes.py::test_reopen_after_close`)
    - .planning/phases/01-foundation-project-palette-pipeline-backbone/01-UI-SPEC.md § Copywriting Contract (the error-copy string the upload tests will assert on)
  </read_first>
  <action>
Create the `tests/test_web/` package.

`tests/test_web/__init__.py` — empty.

`tests/test_web/conftest.py` — four fixtures, each importing `comiccolor.web.*`
*inside* its own body so collection never fails before plan 01-06 lands:
  - `project_dir(tmp_path)` — returns `tmp_path / "Kaito"`, created, with the
    `pages/`, `references/pending/` and `label_maps/` subdirectories, matching
    D-04's folder-per-project layout.
  - `client(project_dir)` — builds the app via `from comiccolor.web.app import create_app`,
    sets `app.state.current_project_path = project_dir`, and yields a
    `fastapi.testclient.TestClient` in a `with` block so lifespan runs.
  - `blank_client()` — same app, `current_project_path` left unset; used by
    `test_requests_without_an_open_project_return_409`.
  - `make_png()` — a factory fixture returning a callable
    `(width, height, colours) -> bytes` that builds a flat-chip PNG in memory with
    Pillow and returns the encoded bytes, for multipart upload payloads.

Then create the three route test modules with skip-marked stubs (same stub shape
as Task 2 — real docstrings, `...` bodies, function-local imports):

`tests/test_web/test_project_routes.py` (marked `filled by plan 01-06`):
  - `test_create_project_makes_folder_layout` (PROJ-01, D-04)
  - `test_reopen_after_close_keeps_pages_and_palette` (PROJ-01, PROJ-05)
  - `test_requests_without_an_open_project_return_409` (RESEARCH.md Pitfall 2)
  - `test_open_rejects_a_path_without_a_project_db` (Security Domain V12)
  - `test_recent_list_skips_folders_that_disappeared` (D-04: the index is never
    authoritative over the folder)
  - `test_close_checkpoints_the_wal` (RESEARCH.md Pitfall 1, resolved Open Question 1)

`tests/test_web/test_page_routes.py` (marked `filled by plan 01-07`):
  - `test_upload_stores_a_server_generated_filename` (PROJ-02, Pitfall 3)
  - `test_upload_rejects_a_non_image_with_4xx` (PROJ-02, Pitfall 4)
  - `test_stage_field_is_panels_after_upload` (PROJ-04, D-07)
  - `test_adding_pages_later_preserves_existing_pages` (PROJ-02)
  - `test_volume_crud_round_trip` (D-03)
  - `test_pipeline_stages_endpoint_lists_eight_stages` (PROJ-04, D-11)

`tests/test_web/test_palette_routes.py` (first three marked `filled by plan 01-08`,
last four marked `filled by plan 01-09`):
  - `test_swatch_upload_creates_colour_n_entries` (PAL-01, D-16)
  - `test_hand_crud_round_trip` (PAL-03)
  - `test_recolour_reports_affected_pages_and_leaves_stage_alone` (PAL-04, D-09)
  - `test_sheet_upload_returns_unpersisted_proposals` (PROJ-03, PAL-02, D-05)
  - `test_accept_creates_entity_and_labelled_entries` (PAL-02, D-05)
  - `test_reject_discards_the_pending_sheet` (PAL-02)
  - `test_sheet_id_with_path_separators_is_rejected` (Security Domain V12, T-01-PATH)

Also add an alias `test_sheet_upload = test_sheet_upload_returns_unpersisted_proposals`
is **not** wanted — instead name the stub exactly `test_sheet_upload_returns_unpersisted_proposals`
and record in the SUMMARY that 01-VALIDATION.md's `::test_sheet_upload` reference
resolves to this test, so the two documents stay reconcilable.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_web -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_web -q` exits 0 and reports 19 skipped, 0 failed, 0 errors
    - `pytest --collect-only -q` over the whole `tests/` tree exits 0
    - `tests/test_web/conftest.py` defines fixtures named `project_dir`, `client`, `blank_client` and `make_png`
    - `grep -n "^from comiccolor\|^import comiccolor" tests/test_web/*.py` returns no matches (all project imports are inside function or fixture bodies)
    - `tests/test_web/test_palette_routes.py` contains stubs marked for both `plan 01-08` and `plan 01-09`
    - `pytest -q` over the full suite exits 0
  </acceptance_criteria>
  <done>`tests/test_web/` exists with shared fixtures and 19 named, skip-marked stubs; the full suite still collects and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → developer machine | Newly declared third-party packages execute install-time and import-time code on the artist's single machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `pip install -e ".[dev,web]"` (fastapi, uvicorn, python-multipart, httpx, scikit-image) | mitigate | All five packages are covered by RESEARCH.md § Package Legitimacy Audit with registry age, repo URL, slopcheck result and an `Approved` disposition; no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries exist, so no install proceeds on an unaudited name. Versions are pinned with a floor (`>=`) matching the live-verified current release, not an invented one. |
| T-01-01 | Tampering | test scaffolding | accept | Test files execute only in the developer's own pytest run; they contain no network, filesystem-write-outside-`tmp_path`, or subprocess behaviour. No trust boundary is crossed. |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` exits 0 with the pre-existing tests passing and 29 new tests skipped.
- `.venv/Scripts/python.exe -m pytest --collect-only -q` exits 0 — no module-level import of a not-yet-written package anywhere in `tests/`.
- `.venv/Scripts/python.exe -c "import fastapi, uvicorn, multipart, httpx, skimage"` exits 0.
</verification>

<success_criteria>
- Every unchecked box in `01-VALIDATION.md` § Wave 0 Requirements that names a Python file now exists on disk (the three frontend boxes belong to plan 01-02).
- `pyproject.toml` declares scikit-image, a `web` extra, and httpx in `dev`.
- The full suite collects and exits 0.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-project-palette-pipeline-backbone/01-01-SUMMARY.md` when done.
Record in it: the installed FastAPI version, whether `hasattr(FastAPI(), "frontend")` was
True (plan 01-06 depends on this), and the exact stub test names created, so downstream
plans can reference them without re-reading the files.
</output>
