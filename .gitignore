.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# Frontend (Vite + TypeScript) build output and dependencies. package-lock.json
# is committed so the resolved dependency tree stays reviewable.
frontend/node_modules/
frontend/dist/

# Studio pages are unreleased material. Never commit them.
data/
workspace/

# Published comic pages used as test input. Copyrighted — never commit them,
# and especially not to a public repo.
test_pages/

# Spike output. The renders are derivative works of the copyrighted test pages,
# so the whole directory stays local rather than trying to glob the images out.
# Findings that matter are written up in flatting-pipeline-spec.md §2.
reports/

# Vendored upstream source and model weights. erika.pth alone is 172 MB;
# these are fetched by setup, not carried in history.
third_party/
*.pth
*.ckpt
*.safetensors

# The running app's workdir: uploaded pages, exported PSDs, server logs.
# Same rule as test_pages/ — the uploads are the artist's material.
.comiccolor-work/

# Claude Code agent worktrees (transient)
.claude/worktrees/

# tokenade — per-project cache / scaffold flag / dedup refs
.tokenade/

# tokenade — legacy per-project embedding model cache (now global)
.fastembed_cache/
