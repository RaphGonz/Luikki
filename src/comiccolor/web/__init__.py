"""The FastAPI layer: app factory, DTOs, per-request Store dependency and routers.

Greenfield for this project (01-PATTERNS.md: "first web code this project has
ever had"). Built directly against RESEARCH.md's Pattern 1 (per-request
``Store``) and Pattern 2 (``app.frontend()`` SPA mount), not an in-repo analog.
"""
