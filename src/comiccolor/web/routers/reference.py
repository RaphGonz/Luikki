"""Reference (character sheet) routes.

PROJ-03 (reference image management) and PAL-02 (character-sheet colour
proposals — proposed, never persisted until the artist accepts). Registered
as an empty stub by plan 01-06; filled by plan 01-10.
"""

from fastapi import APIRouter

router = APIRouter()
