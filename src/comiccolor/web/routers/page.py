"""Page routes.

PROJ-02 (pages live inside a volume) and PROJ-04 (upload, list and view
pages with their pipeline stage). Registered as an empty stub by plan
01-06; filled by plan 01-08 alongside ``volume.py`` and ``pipeline.py``.
"""

from fastapi import APIRouter

router = APIRouter()
