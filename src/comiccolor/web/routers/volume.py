"""Volume routes.

PROJ-02 (organise a project's pages into volumes) and PROJ-04 (pages carry
their pipeline stage). Registered as an empty stub by plan 01-06; filled by
plan 01-08 alongside ``page.py`` and ``pipeline.py``.
"""

from fastapi import APIRouter

router = APIRouter()
