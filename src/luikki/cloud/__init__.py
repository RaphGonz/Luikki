"""The GPU endpoint: Cobra behind an authenticated HTTP route (ROADMAP §B).

The app stays local. Only step 5 crosses the network, one panel per request,
and the zone map never leaves the artist's machine.
"""
